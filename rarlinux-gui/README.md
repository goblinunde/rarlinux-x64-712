# RARLinux GUI

一个基于 Tauri + React 的 WinRAR Linux 图形界面应用程序。

<img src="https://img.shields.io/badge/Tauri-2.0-blue" />
<img src="https://img.shields.io/badge/React-19-61dafb" />
<img src="https://img.shields.io/badge/Rust-1.8-orange" />
<img src="https://img.shields.io/badge/License-Personal_Use-red" />

## 功能特性

✨ **压缩功能**

- 多文件选择
- 可调压缩级别 (0-5)
- AES-256 密码保护
- 分卷压缩支持

📦 **解压功能**

- 支持 RAR/ZIP 格式
- 归档内容预览
- 密码保护解压
- 自定义解压路径

🎨 **现代化界面**

- GNOME 风格设计
- 深色模式自动适配
- 实时进度提示
- 中文界面

## 快速开始

### 安装

**从RPM包安装** (推荐):

```bash
sudo dnf install ./rarlinux-gui-0.1.0-1.x86_64.rpm
```

**从源码编译**:

```bash
# 1. 克隆仓库
cd rarlinux-gui

# 2. 安装依赖
npm install

# 3. 开发模式运行
npm run tauri dev

# 4. 构建RPM包
npm run tauri build
```

### 使用方法

**创建归档**:

1. 点击 "📁 选择文件"
2. 选择要压缩的文件
3. (可选) 设置密码和压缩级别
4. 点击 "➕ 创建归档"

**解压归档**:

1. 点击 "📂 打开归档"
2. 选择 RAR 文件
3. 点击 "📤 解压"
4. 选择目标文件夹

## 技术栈

- **前端**: React 19 + TypeScript + Vite
- **后端**: Rust + Tauri 2.0 + Tokio
- **UI**: 自定义 CSS (GNOME 风格)
- **打包**: RPM (Fedora 43+)

## 项目结构

```
rarlinux-gui/
├── src/                      # React 前端
│   ├── App.tsx              # 主组件
│   └── App.css              # 样式表
├── src-tauri/               # Rust 后端
│   ├── src/
│   │   ├── rar_wrapper.rs   # RAR 封装
│   │   ├── commands.rs      # Tauri 命令
│   │   └── lib.rs           # 模块入口
│   └── resources/           # RAR 二进制
├── package.json             # npm 依赖
└── Cargo.toml               # Rust 依赖
```

## 开发

### 前置要求

- Node.js 18+
- Rust 1.70+
- Tauri CLI 2.0+

### 开发命令

```bash
# 开发模式 (热重载)
npm run tauri dev

# TypeScript 检查
npm run build

# Rust 检查
cd src-tauri && cargo check

# 构建 RPM 包
npm run tauri build
```

## 系统要求

- **操作系统**: Fedora 43+ / RHEL 9+ / 其他 RPM 发行版
- **架构**: x86_64
- **依赖**: libstdc++, glibc

## 许可证

⚠️ **个人研究使用** - 本项目仅供个人学习和研究使用,不得用于商业用途或公开分发。WinRAR 命令行工具受其官方许可证约束。

## 贡献

本项目为个人研究项目,暂不接受外部贡献。

## 作者

SMLYFM <yytcjx@gmail.com>

---

**构建时间**: 2026-01-12  
**版本**: 0.1.0  
**Tauri 版本**: 2.9.5
